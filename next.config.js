/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["https://studio-landing-supersapiensdevlab.vercel.app/"],
  },
};

module.exports = nextConfig;
